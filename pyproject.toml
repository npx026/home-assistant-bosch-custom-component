[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "bosch-thermostat-client"
version = "0.29.0"
dependencies = ["aiohttp", "aioxmpp", "pyaes"]

[tool.setuptools.packages.find]
include = ["bosch_thermostat_client*"]
